import 'package:flutter/material.dart';

class PeriksaKesehatanSendiri extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Priksa Kesehatan Sendiri'),
      ),
    );
  }
}